# -----------------------------------------
# CONFIGURACIÓN DEL SISTEMA DE BACKUP - TIF
# -----------------------------------------


# Cambiá esta ruta por la carpeta real que contiene tu proyecto o archivos importantes
SRC_DIR="/Users/matias/Trabajo-integrador-final-Scripting/scripts"

# Carpeta donde se guardarán los archivos .tar.gz de backup
# Se creará automáticamente si no existe
BACKUP_DIR="$HOME/backups"

# Carpeta donde se guardarán los archivos de log
LOG_DIR="$HOME/tif_logs"

# Cantidad de backups a conservar (los más recientes)
RETENTION=7

# Nivel de verbosidad (0 = silencioso, 1 = mostrar mensajes)
VERBOSE=1

# --------------------
# FIN DE CONFIGURACIÓN
# --------------------
